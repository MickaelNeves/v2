export enum SECTIONS {
  ABOUT = "about",
  EXPERIENCE = "experience",
  PROJECTS = "projects",
}

export enum ANIMATION_TYPES {
  WAVE = "wave",
  SHAKE = "shake",
  SWITCH = "switch",
  VORTEX = "vortex",
}
